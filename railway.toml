[build]
dockerfile = "Dockerfile"

[deploy]
startCommand = "sh -c 'gunicorn -w 2 --timeout 120 -b 0.0.0.0:${PORT:-8000} run:app'"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3